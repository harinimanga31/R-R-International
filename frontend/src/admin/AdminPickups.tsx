import {useEffect,useState} from "react"; import {listPickups,updatePickup} from "../services/pickupApi"; import {Section} from "../components/Section";
export default function AdminPickups(){const [rows,setRows]=useState<any[]>([]);const load=()=>listPickups().then(setRows);useEffect(() => {load();},[]);return <Section><h1 className="text-3xl font-bold">Pickup Requests</h1><div className="card mt-7 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b bg-slate-50"><th className="p-4">Pickup ID</th><th>Customer</th><th>City</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr className="border-b" key={r.id}><td className="p-4">{r.pickup_id}</td><td>{r.customer_name}</td><td>{r.city}</td><td><select className="field w-auto" value={r.status} onChange={async e=>{await updatePickup(r.id,{status:e.target.value});load()}}>{["NEW","SCHEDULED","COLLECTED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select></td></tr>)}</tbody></table></div></Section>}