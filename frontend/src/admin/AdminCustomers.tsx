import {useEffect,useState} from "react"; import {api} from "../services/api"; import {Section} from "../components/Section";
export default function AdminCustomers(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get("/customers").then(r=>setRows(r.data))},[]);return <Section><h1 className="text-3xl font-bold">Customers</h1><div className="card mt-7 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b bg-slate-50"><th className="p-4">Name</th><th>Email</th><th>Phone</th><th>Company</th></tr></thead><tbody>{rows.map(r=><tr className="border-b" key={r.id}><td className="p-4 font-semibold">{r.name}</td><td>{r.email}</td><td>{r.phone}</td><td>{r.company||"—"}</td></tr>)}</tbody></table></div></Section>}