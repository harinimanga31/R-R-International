import { useState } from "react"; import { useNavigate } from "react-router-dom"; import { login } from "../services/authApi";
export default function AdminLogin(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const nav=useNavigate();async function go(e:any){e.preventDefault();setError("");try{const r=await login(email,password);localStorage.setItem("rr_admin_token",r.access_token);nav("/admin/dashboard")}catch(e:any){setError(e?.response?.data?.detail||"Invalid credentials.")}}return <div className="flex min-h-screen items-center justify-center bg-slate-100 p-5"><form onSubmit={go} className="card w-full max-w-md space-y-5 p-8"><img src="/logo.svg" className="h-12"/><h1 className="text-2xl font-bold">Admin Login</h1><input className="field" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn btn-primary w-full">Sign in</button>{error&&<p className="text-sm text-red-600">{error}</p>}</form></div>}