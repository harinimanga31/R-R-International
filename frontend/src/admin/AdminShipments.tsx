import { useEffect,useState } from "react"; import { Link } from "react-router-dom"; import { listShipments,deleteShipment } from "../services/shipmentApi"; import StatusBadge from "../components/StatusBadge"; import { Section } from "../components/Section";
export default function AdminShipments(){const [rows,setRows]=useState<any[]>([]);const load=()=>listShipments().then(setRows);useEffect(()=>{load();},[]);return <Section><div className="flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-bold">Shipments</h1><p className="text-slate-500">Create and manage AWB records.</p></div><Link to="/admin/shipments/new" className="btn btn-primary">New Shipment</Link></div><div className="mt-7 overflow-x-auto card"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">AWB</th><th>Route</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(r=><tr className="border-b" key={r.id}><td className="p-4 font-bold">{r.awb}</td><td>{r.origin} → {r.destination}</td><td><StatusBadge status={r.status}/></td><td><Link className="mr-3 text-brandred" to={`/admin/shipments/${r.id}`}>Edit</Link><button className="text-red-600" onClick={async()=>{if(confirm("Delete shipment?")){await deleteShipment(r.id);load()}}}>Delete</button></td></tr>)}</tbody></table></div></Section>}