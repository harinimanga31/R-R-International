import {useEffect,useState} from "react"; import {api} from "../services/api"; import {Section} from "../components/Section";
export default function AdminDestinations(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get("/destinations").then(r=>setRows(r.data))},[]);return <Section><h1 className="text-3xl font-bold">Destinations</h1><div className="card mt-7 overflow-x-auto"><table className="w-full text-left"><thead><tr className="border-b bg-slate-50"><th className="p-4">Country</th><th>Code</th><th>Active</th></tr></thead><tbody>{rows.map(r=><tr className="border-b" key={r.id}><td className="p-4">{r.country}</td><td>{r.code}</td><td>{r.active?"Yes":"No"}</td></tr>)}</tbody></table></div></Section>}